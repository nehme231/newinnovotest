{
  "name": "icons",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/icons/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project icons --web",
  "targets": {
    "transform": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/icons",
        "command": "rm -rf src/components && mkdir src/components && npx @svgr/cli --out-dir src/components --config-file .svgrrc.js --index-template=src/template/index-template.js  -- src/svg "
      }
    }
  }
}
